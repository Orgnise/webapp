import React from "react";
import {
  FontAwesomeIcon,
  FontAwesomeIconProps,
} from "@fortawesome/react-fontawesome";

export default function FIcon(prop: FontAwesomeIconProps) {
  return <FontAwesomeIcon {...prop} />;
}
